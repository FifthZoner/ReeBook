import React from 'react';
import Hero from '../ui/hero'
import Navbar from '../ui/navbar';

const Welcome = () => {
    return (
        <div>
            <Navbar />
            <Hero />
        </div>
    );
};

export default Welcome;